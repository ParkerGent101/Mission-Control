
  /* ─────────────────────────────────────────────────────────────────────────
     Tokens. A single warm-neutral dark ramp; one accent for chrome. Colour is
     never decorative here — green/red mean only "money in / money out", and the
     four --pace-* steps mean only budget state. Both carry an icon and words
     alongside, so nothing is colour-alone.
     ───────────────────────────────────────────────────────────────────────── */
  :root {
    --bg:        oklch(0.165 0.008 70);
    --bg-2:      oklch(0.198 0.010 70);
    --surface:   oklch(0.225 0.012 70);
    --surface-2: oklch(0.262 0.013 70);
    --surface-3: oklch(0.305 0.014 70);
    --line:      oklch(0.32 0.012 70);
    --line-soft: oklch(0.27 0.011 70);

    --ink:       oklch(0.955 0.008 80);
    --ink-2:     oklch(0.82 0.010 75);
    --ink-3:     oklch(0.62 0.012 70);
    --ink-4:     oklch(0.48 0.012 70);

    --accent:    oklch(0.80 0.135 75);    /* chrome only: focus, active tab */
    --accent-2:  oklch(0.78 0.115 165);   /* money in / the one chart hue */
    --danger:    oklch(0.66 0.18 25);

    /* Reserved status steps — each ≥ 3:1 on --surface, validated as a set. */
    --pace-good:    #0ca30c;
    --pace-warn:    #fab219;
    --pace-serious: #ec835a;
    --pace-crit:    #d03b3b;

    --font-sans: 'IBM Plex Sans', ui-sans-serif, system-ui, sans-serif;
    --font-mono: 'IBM Plex Mono', ui-monospace, monospace;

    --r-sm: 4px;
    --r:    6px;
    --r-lg: 10px;
    --topbar-h: 52px;
  }

  * { box-sizing: border-box; }

  html, body {
    margin: 0; padding: 0;
    background: var(--bg);
    color: var(--ink);
    font-family: var(--font-sans);
    font-size: 14px;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
  }

  body { overflow-x: hidden; }

  h1, h2, h3 { margin: 0; font-weight: 600; }

  /* Every figure is a number in a column of numbers: same width per digit, so
     amounts line up and a changing value doesn't reflow its row. */
  .num, .amount, input.num, td.num {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    font-feature-settings: "tnum" 1;
  }

  .muted   { color: var(--ink-3); }
  .muted-2 { color: var(--ink-4); }
  .pos     { color: var(--accent-2); }
  .neg     { color: var(--danger); }
  .spacer  { flex: 1; }

  code {
    font-family: var(--font-mono); font-size: .92em;
    background: var(--surface-2); border-radius: var(--r-sm); padding: 1px 4px;
  }

  :focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 1px;
  }

  /* ── controls ───────────────────────────────────────────────────────────── */
  .btn {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 6px 10px;
    border-radius: var(--r);
    background: var(--surface-2);
    border: 1px solid var(--line);
    color: var(--ink);
    font-family: var(--font-sans);
    font-size: 12px;
    line-height: 1.3;
    cursor: pointer;
    white-space: nowrap;
    text-decoration: none;
  }
  .btn:hover:not(:disabled) { background: var(--surface-3); }
  .btn:disabled { opacity: .5; cursor: default; }
  .btn.primary {
    background: color-mix(in oklch, var(--accent) 22%, var(--surface-2));
    border-color: color-mix(in oklch, var(--accent) 45%, var(--line));
  }
  .btn.primary:hover:not(:disabled) { background: color-mix(in oklch, var(--accent) 32%, var(--surface-2)); }
  .btn.ghost { background: transparent; border-color: transparent; color: var(--ink-3); }
  .btn.ghost:hover:not(:disabled) { background: var(--surface-2); color: var(--ink); }
  .btn.danger {
    background: color-mix(in oklch, var(--danger) 20%, var(--surface-2));
    border-color: color-mix(in oklch, var(--danger) 50%, var(--line));
    color: var(--ink);
  }
  .btn.inline { padding: 2px 7px; font-size: 11.5px; }
  .btn.icon-only { padding: 5px; min-width: 28px; justify-content: center; }

  .icon-btn {
    display: inline-flex; align-items: center; justify-content: center;
    min-width: 28px; min-height: 28px; padding: 4px;
    background: transparent; border: none; border-radius: var(--r);
    color: var(--ink-3); cursor: pointer;
    font-size: 16px; line-height: 1;
  }
  .icon-btn:hover { background: var(--surface-2); color: var(--ink); }

  .input {
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: var(--r);
    color: var(--ink);
    font-family: var(--font-sans);
    font-size: 13px;
    padding: 5px 8px;
    height: 30px;
    min-width: 0;
  }
  .input:focus { border-color: var(--accent); outline: none; }
  select.input { cursor: pointer; }

  .seg { display: inline-flex; border: 1px solid var(--line); border-radius: var(--r); overflow: hidden; }
  .seg-btn {
    background: transparent; border: none; color: var(--ink-3);
    font-family: var(--font-mono); font-size: 11.5px;
    padding: 4px 9px; cursor: pointer;
  }
  .seg-btn + .seg-btn { border-left: 1px solid var(--line); }
  .seg-btn.on { background: var(--surface-3); color: var(--ink); }

  /* ── shell ──────────────────────────────────────────────────────────────── */
  .app { min-height: 100vh; display: flex; flex-direction: column; }

  .topbar {
    position: sticky; top: 0; z-index: 40;
    height: var(--topbar-h);
    display: flex; align-items: center; gap: 14px;
    padding: 0 16px;
    background: var(--bg-2);
    border-bottom: 1px solid var(--line);
  }
  .brand { display: flex; align-items: center; gap: 8px; }
  .brand-mark {
    width: 9px; height: 9px; border-radius: 2px;
    background: var(--accent);
  }
  .brand-name {
    font-size: 13px; font-weight: 600; letter-spacing: .06em; text-transform: uppercase;
  }
  .topbar-right { margin-left: auto; display: flex; align-items: center; gap: 6px; }

  .month-stepper { display: flex; align-items: center; gap: 4px; margin-left: 8px; }
  .month-label {
    font-family: var(--font-mono); font-size: 13px;
    min-width: 9.5ch; text-align: center; color: var(--ink-2);
  }

  .tabs {
    position: sticky; top: var(--topbar-h); z-index: 30;
    display: flex; gap: 2px;
    padding: 0 12px;
    background: var(--bg);
    border-bottom: 1px solid var(--line);
    overflow-x: auto;
    scrollbar-width: none;
  }
  .tabs::-webkit-scrollbar { display: none; }
  .tab {
    display: inline-flex; align-items: center; gap: 7px;
    padding: 10px 12px;
    background: transparent; border: none;
    border-bottom: 2px solid transparent;
    color: var(--ink-3);
    font-family: var(--font-sans); font-size: 13px;
    cursor: pointer; white-space: nowrap;
  }
  .tab:hover { color: var(--ink-2); }
  .tab.on { color: var(--ink); border-bottom-color: var(--accent); }

  main {
    flex: 1;
    width: 100%; max-width: 1080px; margin: 0 auto;
    padding: 18px 16px 64px;
    display: flex; flex-direction: column; gap: 14px;
  }

  /* ── panels ─────────────────────────────────────────────────────────────── */
  .panel {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--r-lg);
    overflow: hidden;
  }
  .panel-head {
    display: flex; align-items: center; gap: 10px;
    padding: 11px 14px;
    border-bottom: 1px solid var(--line-soft);
  }
  .panel-title { font-size: 13px; font-weight: 600; letter-spacing: .01em; }
  .panel-right { margin-left: auto; display: flex; align-items: center; gap: 6px; flex-wrap: wrap; justify-content: flex-end; }
  .panel-body { padding: 12px 14px 14px; }
  .panel-alerts { border-color: color-mix(in oklch, var(--pace-serious) 35%, var(--line)); }

  .sub-head {
    margin: 14px 0 6px;
    font-size: 11px; letter-spacing: .08em; text-transform: uppercase;
    color: var(--ink-4);
  }

  /* ── stat strip ─────────────────────────────────────────────────────────── */
  .stat-strip {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 10px;
  }
  .stat {
    display: flex; flex-direction: column; gap: 2px;
    padding: 8px 10px;
    background: var(--bg-2);
    border: 1px solid var(--line-soft);
    border-radius: var(--r);
  }
  .stat .l { font-size: 10.5px; letter-spacing: .07em; text-transform: uppercase; color: var(--ink-4); }
  .stat .v { font-size: 19px; font-weight: 500; }

  .pace-summary { margin: 10px 0 0; font-size: 12.5px; color: var(--ink-3); }

  /* ── alerts ─────────────────────────────────────────────────────────────── */
  .alert-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 8px; }
  .alert-row {
    display: flex; align-items: flex-start; gap: 9px;
    padding: 9px 11px;
    background: var(--bg-2);
    border: 1px solid color-mix(in oklch, var(--alert-color, var(--line)) 34%, var(--line));
    border-left: 3px solid var(--alert-color, var(--line));
    border-radius: var(--r);
    color: var(--alert-color, var(--ink));
  }
  .alert-text { color: var(--ink-2); font-size: 12.5px; line-height: 1.55; }
  .alert-text strong { color: var(--ink); font-weight: 600; }

  /* ── budget pace ────────────────────────────────────────────────────────── */
  .pace-legend {
    display: flex; align-items: center; gap: 7px;
    margin-bottom: 10px;
    font-size: 11px; color: var(--ink-4);
  }
  .pace-list { display: flex; flex-direction: column; }
  .pace-row {
    display: grid;
    grid-template-columns: minmax(96px, 1.1fr) minmax(90px, 2.4fr) auto auto 74px;
    align-items: center; gap: 10px;
    padding: 7px 0;
    border-top: 1px solid var(--line-soft);
  }
  .pace-row:first-child { border-top: none; }
  .pace-name {
    font-size: 12.5px; color: var(--ink-2);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .pace-track {
    position: relative; height: 8px;
    background: var(--bg-2);
    border-radius: 999px;
    overflow: visible;
  }
  .pace-fill {
    height: 100%;
    border-radius: 999px 4px 4px 999px;   /* 4px rounded data-end, anchored at the baseline */
    min-width: 3px;
    transition: width .2s ease;
  }
  /* The "even spend rate would be here today" marker. */
  .pace-tick {
    position: absolute; top: -3px; width: 2px; height: 14px;
    background: var(--ink-2);
    border-radius: 1px;
    transform: translateX(-1px);
  }
  .pace-tick.static { position: static; display: inline-block; transform: none; }
  .pace-over {
    position: absolute; right: -6px; top: -2px;
    width: 0; height: 0;
    border-left: 5px solid var(--pace-crit);
    border-top: 6px solid transparent;
    border-bottom: 6px solid transparent;
  }
  .pace-amt    { font-size: 12.5px; text-align: right; }
  .pace-budget { font-size: 11.5px; text-align: right; }
  .pace-status {
    display: inline-flex; align-items: center; justify-content: flex-end; gap: 5px;
    font-family: var(--font-mono); font-size: 11.5px;
  }

  /* ── generic list rows ──────────────────────────────────────────────────── */
  .line-item {
    display: grid; grid-template-columns: 1fr auto auto;
    align-items: center; gap: 10px;
    padding: 6px 0;
  }
  .line-item-name { font-size: 13px; }
  .line-item-meta { font-size: 11px; color: var(--ink-4); }

  .editor {
    display: flex; flex-direction: column; gap: 8px;
    margin-top: 10px; padding: 11px;
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: var(--r);
  }
  .editor-label { font-size: 11.5px; color: var(--ink-3); }
  .editor-row { display: flex; gap: 7px; align-items: center; flex-wrap: wrap; }
  .editor-actions { display: flex; gap: 7px; margin-top: 2px; }

  /* ── transactions ───────────────────────────────────────────────────────── */
  .add-form {
    display: flex; gap: 8px; flex-wrap: wrap; align-items: center;
    padding-bottom: 12px; margin-bottom: 10px;
    border-bottom: 1px solid var(--line-soft);
  }
  .txn-count { font-size: 11.5px; margin-bottom: 4px; }

  .txn-group-head {
    display: flex; align-items: center; gap: 8px; width: 100%;
    padding: 8px 2px;
    background: transparent; border: none;
    border-top: 1px solid var(--line-soft);
    color: var(--ink-2); font-family: var(--font-sans);
    cursor: pointer; text-align: left;
  }
  .txn-group-head:hover { color: var(--ink); }
  /* Drawn rather than a ▾ glyph — IBM Plex has no geometric-shapes block, so the
     character fell back to a dot. */
  .caret {
    display: inline-block; width: 0; height: 0; flex-shrink: 0;
    margin: 0 3px 0 1px;
    border-left: 4px solid transparent;
    border-right: 4px solid transparent;
    border-top: 5px solid var(--ink-4);
    transition: transform .12s;
  }
  .caret.shut { transform: rotate(-90deg); }
  .txn-group-name {
    font-size: 11px; letter-spacing: .06em; text-transform: uppercase;
  }

  .txn {
    display: grid;
    grid-template-columns: 1fr 132px 96px 32px;
    align-items: center; gap: 8px;
    padding: 5px 2px 5px 19px;
  }
  .txn:hover { background: var(--bg-2); }
  .txn-main { min-width: 0; }
  .txn-name { font-size: 13px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .txn-date { font-size: 11px; }
  .txn-amt { font-size: 12.5px; text-align: right; }
  .editable { cursor: pointer; }
  .editable:hover { text-decoration: underline dotted; text-underline-offset: 3px; }
  .txn-cat {
    font-family: var(--font-mono); font-size: 10.5px;
    height: 24px; padding: 2px 5px;
    background: var(--surface-2); border: 1px solid var(--line);
    border-radius: var(--r); color: var(--ink-3); cursor: pointer;
    max-width: 100%;
  }
  .txn-cat-static { font-family: var(--font-mono); font-size: 10.5px; }

  /* ── bills ──────────────────────────────────────────────────────────────── */
  .cal-grid {
    display: grid; grid-template-columns: repeat(7, minmax(0, 1fr)); gap: 3px;
  }
  .cal-dow {
    text-align: center; font-size: 10px; letter-spacing: .08em;
    color: var(--ink-4); padding-bottom: 3px;
  }
  .cal-pad { min-height: 10px; }
  .cal-day {
    min-height: 62px; padding: 4px;
    background: var(--bg-2);
    border: 1px solid var(--line-soft);
    border-radius: var(--r-sm);
    display: flex; flex-direction: column; gap: 2px;
    overflow: hidden;
  }
  .cal-day.has-bills { background: var(--surface-2); }
  .cal-day.today { border-color: var(--accent); }
  .cal-daynum { font-family: var(--font-mono); font-size: 10px; color: var(--ink-4); }
  .cal-day.today .cal-daynum { color: var(--accent); }
  .cal-bill {
    display: flex; align-items: center; gap: 3px;
    font-size: 9.5px; line-height: 1.3;
    color: var(--bill-color, var(--ink-3));
    border-left: 2px solid var(--bill-color, var(--line));
    padding-left: 3px;
  }
  .cal-bill-name {
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
    color: var(--ink-2);
  }
  .cal-total { margin-top: auto; font-size: 9.5px; text-align: right; }

  .bill-list { display: flex; flex-direction: column; }
  .bill-row {
    display: grid; grid-template-columns: 28px 1fr auto 96px;
    align-items: center; gap: 10px;
    padding: 7px 2px;
    border-top: 1px solid var(--line-soft);
  }
  .bill-row:first-child { border-top: none; }
  .bill-day { font-size: 11.5px; }
  .bill-main { min-width: 0; }
  .bill-name { font-size: 13px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .bill-meta { font-size: 11px; }
  .bill-status {
    display: inline-flex; align-items: center; gap: 5px;
    font-family: var(--font-mono); font-size: 10.5px;
  }
  .bill-amt { font-size: 12.5px; text-align: right; }

  /* ── accounts ───────────────────────────────────────────────────────────── */
  .networth { display: flex; gap: 20px; align-items: center; flex-wrap: wrap; }
  .networth-figure { display: flex; flex-direction: column; gap: 3px; min-width: 190px; }
  .networth-value { font-size: 30px; font-weight: 500; line-height: 1.15; }
  .networth-change { font-size: 12px; }
  .chart-wrap { flex: 1; min-width: 240px; display: flex; flex-direction: column; gap: 4px; }
  .networth-chart { width: 100%; height: 118px; display: block; }
  .chart-caption { font-size: 11px; text-align: right; }

  .acct-list { display: flex; flex-direction: column; }
  .acct-row {
    display: grid; grid-template-columns: 1fr auto auto;
    align-items: center; gap: 12px;
    padding: 8px 2px;
    border-top: 1px solid var(--line-soft);
  }
  .acct-row:first-child { border-top: none; }
  .acct-main { min-width: 0; }
  .acct-name { font-size: 13px; }
  .acct-meta { font-size: 11px; }
  .acct-change { font-size: 11.5px; }
  .acct-balance { font-size: 14px; }

  /* ── trends ─────────────────────────────────────────────────────────────── */
  .sparks {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(168px, 1fr)); gap: 12px;
  }
  .spark {
    padding: 9px 10px;
    background: var(--bg-2);
    border: 1px solid var(--line-soft);
    border-radius: var(--r);
  }
  .spark-head { display: flex; align-items: baseline; gap: 6px; margin-bottom: 3px; }
  .spark-name {
    font-size: 12px; color: var(--ink-2);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .spark-delta { margin-left: auto; font-size: 11px; }
  .spark-foot { font-size: 10.5px; margin-top: 2px; }
  .mini-line { display: block; width: 100%; height: auto; }
  .mini-line-empty { background: var(--surface-2); border-radius: var(--r-sm); }

  .bars { display: flex; align-items: flex-end; gap: 10px; flex-wrap: wrap; }
  .bar-col { flex: 1; min-width: 54px; display: flex; flex-direction: column; align-items: center; gap: 4px; }
  .bar-pair {
    display: flex; align-items: flex-end; gap: 2px;   /* 2px surface gap between adjacent marks */
    height: 118px; width: 100%; justify-content: center;
  }
  .bar { width: 42%; max-width: 26px; min-height: 2px; border-radius: 4px 4px 0 0; }
  .bar.in  { background: var(--accent-2); }
  .bar.out { background: var(--ink-3); }
  .bar-label { font-size: 10.5px; letter-spacing: .04em; }
  .bar-net { font-size: 10.5px; }
  .bars-legend {
    flex-basis: 100%; display: flex; gap: 14px; justify-content: center;
    margin-top: 6px; font-size: 11px; color: var(--ink-3);
  }
  .bars-legend span { display: inline-flex; align-items: center; gap: 5px; }
  .bars-legend .swatch { width: 9px; height: 9px; border-radius: 2px; display: inline-block; }
  .bars-legend .swatch.in  { background: var(--accent-2); }
  .bars-legend .swatch.out { background: var(--ink-3); }

  /* ── settings ───────────────────────────────────────────────────────────── */
  .st-overlay {
    position: fixed; inset: 0; z-index: 200;
    background: rgba(0,0,0,.62);
    display: flex; align-items: center; justify-content: center;
    padding: 20px;
  }
  .st-panel {
    width: min(560px, 100%); max-height: 86vh; overflow-y: auto;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--r-lg);
  }
  .st-head {
    position: sticky; top: 0;
    display: flex; align-items: center; gap: 10px;
    padding: 13px 15px;
    background: var(--surface);
    border-bottom: 1px solid var(--line-soft);
  }
  .st-head h2 { font-size: 14px; }
  .st-head .btn { margin-left: auto; }
  .st-body { padding: 4px 15px 18px; }
  .st-section { padding: 14px 0; border-bottom: 1px solid var(--line-soft); }
  .st-section:last-child { border-bottom: none; }
  .st-section h3 { font-size: 12.5px; margin-bottom: 5px; }
  .st-hint { margin: 0 0 10px; font-size: 12px; color: var(--ink-3); line-height: 1.55; }
  .st-warn {
    margin: 8px 0; padding: 8px 10px; font-size: 12px; line-height: 1.55;
    background: color-mix(in oklch, var(--pace-warn) 12%, var(--bg-2));
    border: 1px solid color-mix(in oklch, var(--pace-warn) 35%, var(--line));
    border-radius: var(--r); color: var(--ink-2);
  }
  .st-status { display: flex; align-items: center; gap: 8px; font-size: 12.5px; margin-bottom: 10px; }
  .st-dot { width: 8px; height: 8px; border-radius: 50%; }
  .st-dot.ok  { background: var(--pace-good); }
  .st-dot.off { background: var(--ink-4); }
  .st-field { display: flex; flex-direction: column; gap: 4px; margin-bottom: 9px; }
  .st-field span { font-size: 11.5px; color: var(--ink-3); }
  .st-actions { display: flex; gap: 7px; flex-wrap: wrap; }

  /* ── toasts ─────────────────────────────────────────────────────────────── */
  .toasts {
    position: fixed; bottom: 18px; right: 16px; z-index: 300;
    display: flex; flex-direction: column; gap: 7px;
    pointer-events: none; max-width: min(360px, calc(100vw - 32px));
  }
  .toast {
    padding: 8px 12px;
    background: var(--surface-3);
    border: 1px solid var(--line);
    border-radius: var(--r);
    font-size: 12.5px; line-height: 1.45;
    box-shadow: 0 6px 18px rgba(0,0,0,.4);
    animation: toastIn .16s ease-out;
  }
  .toast-success { border-color: color-mix(in oklch, var(--accent-2) 45%, var(--line)); }
  .toast-error   { border-color: color-mix(in oklch, var(--danger) 50%, var(--line)); color: var(--ink); }
  @keyframes toastIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: none; } }

  @keyframes spin { to { transform: rotate(360deg); } }

  /* ── responsive ─────────────────────────────────────────────────────────── */
  @media (max-width: 720px) {
    main { padding: 14px 11px 56px; gap: 11px; }
    .topbar { gap: 8px; padding: 0 11px; }
    .brand-name { display: none; }
    .month-stepper { margin-left: 0; }
    .btn-text { display: none; }
    .tab span { display: none; }
    .tab { padding: 10px 14px; }
    .panel-body { padding: 11px; }

    /* The bar and the tick are the least useful things on a phone; the numbers
       and the status word are the most. */
    /* Title gets its own row so the action buttons stop shouldering it aside. */
    .panel-head { flex-wrap: wrap; row-gap: 8px; }
    .panel-title { flex-basis: 100%; }
    .panel-right { margin-left: 0; justify-content: flex-start; }

    .pace-row { grid-template-columns: 1fr auto auto; row-gap: 3px; }
    .pace-track { grid-column: 1 / -1; order: 3; }
    .pace-budget { display: none; }

    .txn { grid-template-columns: 1fr auto 28px; padding-left: 12px; row-gap: 3px; }
    /* Both the picker and the plain income label drop to their own row — otherwise
       a row with four children overflows a three-column grid and wraps the ×. */
    .txn-cat, .txn-cat-static {
      grid-column: 1 / -1; order: 3; justify-self: start; width: auto; max-width: 100%;
    }
    .txn-amt { white-space: nowrap; }

    .bill-row { grid-template-columns: 24px 1fr 88px; row-gap: 2px; }
    .bill-status { grid-column: 2 / -1; }

    .cal-day { min-height: 46px; }
    .cal-bill-name, .cal-total { display: none; }

    .networth-value { font-size: 25px; }
    .sparks { grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); }
  }

  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { animation-duration: .01ms !important; transition-duration: .01ms !important; }
  }
